import 'package:flutter/widgets.dart';

import 'navigation.dart';

double get appScreenHeight => MediaQuery.of(AppNav.i.navContext).size.height;

double get appScreenWidth => MediaQuery.of(AppNav.i.navContext).size.height;
